<div class="relative mb-6 w-full">
    <h1 class="text-xl font-semibold text-zinc-900">{{ __('Settings') }}</h1>
    <p class="mb-6 mt-1 text-zinc-500">{{ __('Manage your profile and account settings') }}</p>
    <hr class="border-zinc-200">
</div>
